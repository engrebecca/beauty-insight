import React from 'react';
import { Spinner } from 'reactstrap';

const Spin = (props) => {
    return (
        <div>
            <Spinner color="danger" />
        </div>
    );
}

export default Spin;